DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres

# Must match apps/api's value (A2.4) — the seed script encrypts the dev
# site_secret with this key, and only apps/api decrypts it at runtime.
SITE_SECRET_ENCRYPTION_KEY=
