# Local Postgres — from `supabase start`, see LOCAL-DEVELOPMENT-SETUP.md §4
DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres
PORT=4000

# Supabase Auth (local) — printed by `supabase start`
SUPABASE_URL=http://localhost:54321
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Local object storage (MinIO, see §5) — NOT real Cloudflare R2 credentials
R2_ACCESS_KEY_ID=minioadmin
R2_SECRET_ACCESS_KEY=minioadmin
R2_BUCKET_NAME=syntaxwp-artifacts-dev
R2_ENDPOINT=http://localhost:9000

# Shared secret the local Cloudflare Worker probe uses to call this API
CF_WORKER_SECRET=dev-local-secret-change-me

# AES-256 key (base64, 32 raw bytes) for encrypting sites.site_secret_ciphertext
# at rest (A2.4) — generate your own per-environment value, never reuse this one:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SITE_SECRET_ENCRYPTION_KEY=

# Reserved for later tasks (Track B LLM router, billing) — optional for now
DEEPSEEK_API_KEY=
GEMINI_API_KEY=
RESEND_API_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
